
// === Test Deployment File ===
// Created via Google Apps Script ? GitHub ? Vercel

export default function handler(req, res) {
  res.status(200).json({ message: "? Hello from Vercel via Apps Script!" });
}
